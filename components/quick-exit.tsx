"use client";
export function QuickExit(){function leave(){try{document.documentElement.setAttribute("data-mercy-quick-exit-guard","1");document.cookie.split(";").forEach(c=>{const n=c.split("=")[0].trim();if(n.startsWith("sb-"))document.cookie=`${n}=; Max-Age=0; path=/; SameSite=Lax`});localStorage.clear();sessionStorage.clear();sessionStorage.setItem("mercy_quick_exit","1")}finally{window.location.replace("/auth")}}return <button className="btn secondary" onClick={leave} aria-label="Быстро скрыть приватную страницу">Быстрый выход</button>}

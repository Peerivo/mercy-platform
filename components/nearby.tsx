"use client";
import {useState} from "react";import {browserSupabase} from "@/lib/supabase/client";
type Place={id:string;organization_name:string;name:string;city:string;address_public:string|null,languages:string[],formats:string[],cost_type:string,distance_meters:number|null,contact_public:string|null,opening_hours:string|null};
export function Nearby({initial}:{initial:Place[]}){const [places,setPlaces]=useState(initial),[status,setStatus]=useState(""),[map,setMap]=useState(false);async function locate(){if(!navigator.geolocation){setStatus("Геолокация недоступна. Используйте фильтр города.");return}setStatus("Запрашиваем местоположение…");navigator.geolocation.getCurrentPosition(async p=>{const {data,error}=await browserSupabase().rpc("nearby_service_locations",{user_lat:p.coords.latitude,user_lon:p.coords.longitude,radius_m:50000,result_limit:50,result_offset:0});if(error)setStatus("Не удалось выполнить поиск. Список остаётся доступен.");else{setPlaces(data||[]);setStatus("Расстояние указано по прямой.")}},()=>setStatus("Доступ к геолокации не дан. Выберите город — каталог продолжает работать."),{timeout:8000})}return <><div className="nav"><button className="btn" onClick={locate}>Найти рядом со мной</button><button className="btn secondary" onClick={()=>setMap(true)}>Загрузить внешнюю карту</button></div><p aria-live="polite">{status}</p>{map&&<div className="notice">Карта использует провайдера, указанного в настройках. В MVP точки доступны списком даже при ошибке тайлов. Координаты посетителя не сохраняются и не добавляются в URL.</div>}<div className="grid cols2">{places.length?places.map(p=><article className="card" key={p.id}><h2>{p.organization_name}: {p.name}</h2><p>{p.city}{p.address_public?`, ${p.address_public}`:""}</p><p>{p.formats.join(", ")} · {p.cost_type} · {p.languages.join(", ")}</p>{p.distance_meters!=null&&<p>{(p.distance_meters/1000).toFixed(1)} км по прямой</p>}<p>{p.opening_hours||"Часы работы не уточнены"}</p><p>{p.contact_public}</p></article>):<div className="card">Проверенных опубликованных организаций пока нет. Мы не показываем непроверенные контакты.</div>}</div></>}

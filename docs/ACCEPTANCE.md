# Acceptance

## Automated status
- Application container dependency coverage must remain provable: the repository must not contain `compose.yaml`, `compose.yml`, `docker-compose.yaml`, or `docker-compose.yml`; CI/Vercel/REG.RU build from the digest-pinned `Dockerfile`, while Beget's self-hosted Supabase Compose is remote infrastructure discovered through Docker labels rather than a repository application manifest.
- The production `Repair Beget Auth URLs` job must remain `workflow_dispatch`-only, exact-current-`main` gated, confirmed with `REPAIR_AUTH_URLS`, and the only job with `environment: production`. A push trigger is allowed only for `test/auth-repair-workflow-*` and must run an isolated `branch-test` job with no production Environment, no Beget secrets, no SSH, and no production mutation; it must syntax-check the repair scripts, run focused Auth tests, build the production app with canonical site URL, and verify the callback surface against a local internal-origin probe.
- `.peerivo/global-contract.json` must exist and bind `Peerivo/mercy-platform` to the current canonical Global Contract authority with the exact required version, digest and Git SHA; agents must resolve the authority from the binding before effectful work and fail closed for missing/stale authority-changing bindings.
- Unit validation: help request, volunteer offer, moderation and staff workspace schemas are covered. Specialist/provider validation is intentionally absent because that contour is not part of Mercy.
- Clean temporary Supabase migration/RLS/Realtime integration is mandatory in CI and runs without cloud credentials or silent skips.
- pgTAP: 32 schema/RLS/grant/default-privilege/publication assertions, including explicit checks that `specialist_profiles`, qualification documents, the specialist projection and specialist RPCs do not exist after clean migration replay.
- Auth/API/Realtime: 10 behavioral tests with isolated real user sessions, including nonce conflicts, concurrent per-author quota, anonymous EXECUTE denial, PostgreSQL readiness, denied access after reassignment, and open-socket reassignment/staff-role revocation scenarios.
- Chromium: production-build browser acceptance covers session isolation, request/chat flows, Quick Exit, ordinary Back/BFCache safety, responsive shell, icon metadata and horizontal-overflow protection.
- Header auth UX: anonymous users see a single `Вход` entry instead of a permanent `Кабинет`; after sign-in the same entry becomes `Кабинет`, including the mobile menu. The global header never shows `Быстрый выход`; `/cabinet` exposes `Выйти из аккаунта`, while Quick Exit remains available only when the user has private access to a request/chat.
- Auth callback origin: every callback redirect, including missing/invalid code and successful exchange with `next`, must resolve from canonical `NEXT_PUBLIC_SITE_URL`, never from `Request.url` or proxy-supplied internal origin. Tests must cover an incoming `https://0.0.0.0:3000/auth/callback` request and require public redirects under `https://mercy.peerivo.net`; `next` must remain on the canonical origin after WHATWG URL normalization, including scheme-relative and backslash-normalized inputs.
- Security remediation acceptance: lockfile resolves Playwright `1.55.1`, Vitest `4.1.0` and nested Supabase CLI `tar` `7.5.22`; Dockerfile base stages are digest-pinned and use no package-manager-installed healthcheck tool. Every external `uses:` reference in `.github/workflows` must be a 40-character immutable commit SHA, and workflows must not invoke package binaries through `npx`, `npm exec`, `pnpm dlx` or `yarn dlx`; CI regression tests enforce both. Machine-managed threat locks may be released only by authoritative full Threat Radar rescan.
- Local lint, typecheck, unit tests and production build must pass before publication; Docker-backed checks remain mandatory in CI and coverage is not weakened.
- Responsive shell: header and main content use the same bounded container with 16/24/40/56 px inline padding at the 0/640/1024/1440 px breakpoints.
- SEO/discovery browser acceptance must verify normalized canonical URLs, `/robots.txt`, `/sitemap.xml`, observable `noindex` on non-indexable public/private utility routes, and self-referencing pagination canonical for `/requests?page=N`; robots exclusions must not prevent crawlers from observing required `noindex` directives.
- Russian Supabase migration preflight is read-only: it must accept non-sensitive Beget host/user from the production Environment Variables (with secret fallback), keep credentials/keys in Secrets, validate pinned SSH access, source/target PostgreSQL versions, an empty target Mercy contour including zero Storage buckets/objects, and availability of required extensions without dumping user data or modifying the Beget database. An extension does not have to be installed on the fresh target if the Mercy migration itself installs it; it must be present in `pg_available_extensions`. Source DB credentials must not appear in process arguments or logs.

- REG.RU application deploy/cutover must use `PREPARE` with exact approved live-main SHA, pinned SSH trust, canonical `https://api.mercy.peerivo.net`, a valid public Beget key before SSH, immutable image, and rollback armed before destructive container replacement. Before promotion, the candidate must pass local `/health`, bounded `/health/data`, Russian-host 308 redirect proof, and an internal-origin Auth callback smoke whose `Location` is exactly `https://mercy.peerivo.net/auth?error=callback`; failure must occur while rollback is still armed. PREPARE must not mutate DNS. `VERIFY` remains public-read-only and must prove exact public SHA, `/health/data=ok`, Beget Auth reachability, and HTTP 308 from `язык-милосердия.рф` to `https://mercy.peerivo.net` with path/query preserved. The workflow must never run database migration tooling.

## Mercy product boundary
Mercy contains public help requests with private contact data, owner actions, staff coordination, volunteer offers, complaint moderation and a verified organization/location catalog.

Mercy does **not** contain professional or medical provider profiles, qualification documents, licence verification, medical booking, provider matching, payments, commissions, ratings or a medical marketplace. Clean migration replay must end with those specialist/provider database objects absent.

## Required behavioral run
On disposable Supabase create users, coordinators, one admin and explicitly fictional data. User A registers/signs in and creates a request; admin queue exposes only the routing minimum and assigns a coordinator with reason; owner/coordinator exchange idempotent messages and perform valid status transitions. Another user, unassigned coordinators and forged ownership/author/role/assignment mutations fail.

Reassignment must revoke the previous coordinator's RLS/Realtime access and grant the new coordinator access. Staff-role revocation must likewise remove access from existing and reconnected Realtime subscriptions.

Catalog tests publish only verified organization+point records; pending rows stay hidden. Known coordinates verify ordering/distance, radius and pagination. Confidential locations never expose protected addresses.

At mobile width and keyboard-only, labels, focus, loading/error/empty states and horizontal overflow remain usable. Quick Exit must not wait for network and must prevent private content/session recovery through Back/BFCache behavior.

## Volunteer offer MVP
A user creates an offer only through the atomic RPC: owner identity comes from JWT, bounded payload/rate limits are checked, and consent is recorded. Direct INSERT is revoked. RLS exposes a row only to its owner and ADMIN; creating an offer never grants staff privileges. ADMIN moderation requires a reason and writes audit data without copying private contact/description into the audit log.

## Production launch gate
Before LIVE: apply migration history to the explicitly identified Supabase project through the protected GitHub Actions workflow, verify backup/restore, Auth Site URL/redirect+SMTP/email confirmation, Realtime publication, trusted ADMIN bootstrap, immutable image SHA and smoke tests. No manual `supabase db push` is part of the production procedure.

Before the Russian Beget cutover, the read-only preflight must pass first. The cutover smoke checks must use the canonical public Beget API URL `https://api.xn----htbcggcjkhwxk7j6bn.xn--p1ai` directly rather than requiring it as a secret. The destructive cutover must require explicit `MIGRATE`, re-check source migration history and Auth compatibility, reject non-email/MFA/SSO/Storage profiles, take a safety backup of Beget, freeze all Mercy public writes plus Auth user/identity writes on the old source in one transaction, stream that SQL through attached Docker stdin, arm idempotent cleanup before execution, and verify coverage by the exact expected relation set rather than a fragile aggregate count, apply canonical repository migrations, Auth users/identities, public data and migration history in one target transaction executed by a non-interpolating remote shell, normalize the historical specialist Storage bucket to the verified empty source Storage profile by temporarily setting `session_replication_role = replica` only around deletion of the known empty bucket inside the bootstrap transaction, immediately restore `origin`, compare deterministic row fingerprints plus RLS and Realtime publication state, verify Auth/REST/Realtime/Storage/Kong, automatically restore the current Beget safety backup after any post-apply failure by recreating the target database before `pg_restore`, preserving archived ownership/ACL entries, behaviorally verifying Auth/REST/Storage database-role access, and automatically unfreeze the old source on any failure. A previously failed/interrupted post-apply run may be recovered only by an explicit numeric `recover_from_run_id` that GitHub Actions independently verifies as a main-branch Mercy cutover ending in `failure`, `timed_out`, or `cancelled`; recovery must fail closed after a successful-cutover marker exists. New safety backups must include database creation metadata, a `prepared` state marker, and a server-side fingerprint of database owner/ACL/settings before any destructive restore is allowed; the historical legacy compatibility archive run `35770879007` was restored once and is permanently quarantined from further restore. CI must exercise the restore against disposable PostgreSQL, verify preserved database settings/ownership/grants, and verify an unrelated role still cannot connect when `PUBLIC CONNECT` was revoked. Service restart must resolve containers by Docker Compose labels and must not require filesystem access to the root-owned compose directory. On success the old source stays frozen until the Russian application cutover is verified. The procedure is documented in `docs/RU_SUPABASE_MIGRATION.md`.

Recovery must verify the local `supabase_admin` role is a superuser before database drop and use it for database-level setting restoration; CI must restore a harmless custom GUC in disposable Supabase and execute the production role verifier. Restore errors must suppress SQL that can contain secrets; safe diagnostics may emit only a fixed error category and numeric archive TOC id, with no raw SQL, parameter values, or persistent stderr file. After failed recovery run #8, no target freshness may be asserted until a successful recovery and read-only preflight; a legacy archive without a saved fingerprint must never use the partially restored target's metadata as expected backup metadata. 

Recovery-only acceptance: `RECOVER` with an independently verified failed/interrupted run ID restores and checks a fresh Beget baseline without executing `MIGRATE` or freezing source. The read-only preflight rejects nonzero target `auth.users`, a Mercy relation marker, Storage buckets, or Storage objects. A separate fresh approval is required for any later `MIGRATE`. The disposable integration scenario creates Mercy objects plus Auth and Storage data after a baseline backup, simulates failure, restores, then checks zero users/buckets/objects, absent `public.help_requests`, and role-level Auth/REST/Storage access.

A repeated recovery failure must be diagnosed from the retained archive index without a database mutation or raw SQL/secret output before another restore attempt; the protected read-only inspection workflow verifies failed-run identity and reports only one requested TOC metadata entry.

The read-only inspection of the retained run `35770879007` archive identified TOC `4300` as an ACL entry for `graphql_public.graphql(text,text,jsonb,jsonb)`. Run `35832828340` subsequently inspected the related TOC metadata and target schema/wrapper/extension flags. The inspector must fail closed on archive-listing errors, expose no SQL or parameter values, and perform no database mutation. Those results established that the wrapper is absent while schema and extension exist.

Run `35832828340` proved the retained archive has GraphQL schema/extension and the wrapper ACL but no wrapper FUNCTION entry; the target has the schema and installed extension while the wrapper is absent. Restore must defer only that ACL, restore the rest of the archive with ownership/privileges, recreate the canonical wrapper under `supabase_admin` only if missing, replay the exact archived ACL, and check archived API-role function grants, owner and invocation by the owner without widening schema privileges. A disposable extension-member archive must reproduce this missing-definition/retained-ACL shape and pass full clean restore. The temporary push trigger was removed; freshness was unverified until protected RECOVER and independent preflight succeeded.

The split TOC restore must also replay the archive's own database-level GRANT/REVOKE commands from `--create` metadata: `--use-list` otherwise omits them. CI verifies a stranger role cannot CONNECT after the GraphQL ACL and database ACL passes. The command text and database settings must stay out of logs.

Run `35836636954` showed that the retained baseline denies `anon` schema USAGE on `graphql_public`; requiring an `anon` invocation would change the archive's ACL contract. Recovery must preserve that restriction, verify wrapper owner/function ACL, invoke the wrapper as `supabase_admin`, and keep the independent Auth/REST/Storage role verifier. The target remained unverified until RECOVER and preflight passed.

## Verified recovery evidence and remaining gate (2026-09-23)
`RECOVER` run `35837560999` restored retained run `35770879007` backup and passed fresh-target/service-role checks without source freeze or MIGRATE. Independent preflight `35837818721` passed with target users `0`, absent `public.help_requests`, buckets `0`, objects `0`; source query confirmed 8 users, 0 freeze triggers and no `mercy_migration` schema. Backups remain retained. The run #8 Actions log exposed the Beget JWT signing setting. Before a fresh MIGRATE approval, rotate that signing secret, regenerate and deploy dependent anon/service keys across Beget and GitHub, restart services, verify old JWTs fail and Auth/REST/Storage work, restrict/delete old logs, and record secret-free evidence. Only then set the protected production Environment variable `BEGET_JWT_ROTATION_ATTESTATION=rotated-and-verified-after-run-8`. Workflow and script must reject MIGRATE without it before SSH/source freeze; the old run `35770879007` backup must be permanently quarantined from both RECOVER and MIGRATE, even without attestation. Production Environment must restrict deployments to protected main, to prevent dispatching older ungated refs; current scripts must also reject other refs. Log cleanup alone is insufficient. A later MIGRATE always requires a separate fresh explicit approval. Global Review was SKIPPED under the bounded exception, with Threat Radar active.

If post-rotation verification reports `DB_JWT_STALE`, production SQL must not be entered manually. The protected `Repair Beget JWT database setting` workflow must run only from reviewed `main` in the `production` Environment and only after a fresh scoped approval. It derives the already-rotated signing secret from running Auth/REST containers, requires those values to agree (and Storage too when its JWT env is present), never accepts the secret as a GitHub input/secret, never prints it, applies the database-level setting with `ALTER DATABASE postgres SET "app.settings.jwt_secret" FROM CURRENT` using stdin/session state rather than embedding the value in SQL or process arguments, verifies the stored value against the runtime secret on a new connection, and otherwise fails closed. Public Auth/REST/Storage checks and rejection of the old key are still required after this repair before setting the attestation.


## Cutover fingerprint regression after run 13

Run `35899437604` failed safely after apply because the target fingerprint loop emitted only the first public table plus Auth relations. When a target fingerprint loop reads table names from process substitution, every SSH invocation in that loop must use `-n` (or an equivalent explicit stdin detachment) so SSH cannot consume the remaining table-list input. CI must assert this invariant. A failed comparison must still trigger target safety-backup restore and source unfreeze. Before any retry, read-only evidence must prove the source is unfrozen and Beget is fresh; the retry requires a separate fresh production MIGRATE approval.


## Production Auth URL repair hardening
When the remote repair body is streamed to `bash -s`, every Docker Compose command that does not intentionally consume data from stdin must explicitly detach stdin (for example with `</dev/null`) before any host mutation; a disposable preflight container must not be able to consume the remaining script and falsely end the step successfully. Callback verification must follow at most four 307/308 redirects, must refuse to follow any cross-origin hop, may traverse only the canonical callback path during normalization, and succeeds only when the same-origin target is `/auth` or `/auth/` with query parameter `error=callback`; loops, lookalike origins, wrong paths and non-redirect responses fail closed.

A production Auth URL repair must fail before host mutation unless the exact staged Compose model renders all four canonical values: `API_EXTERNAL_URL=https://api.mercy.peerivo.net/auth/v1`, `GOTRUE_SITE_URL=https://mercy.peerivo.net`, `GOTRUE_URI_ALLOW_LIST=https://mercy.peerivo.net/auth/callback`, and `GOTRUE_JWT_ISSUER=https://api.mercy.peerivo.net/auth/v1`. The workflow must tolerate stale temporary Compose labels left by earlier fail-closed recreates by rediscovering the standard root-owned Compose files under the canonical working directory; it must not widen host permissions or require passwordless sudo.

The repair must use a final Auth-only temporary Compose override rather than editing unknown legacy Compose source. It may write only the protected env file before the Auth-only recreate, must preserve the Auth image and all non-URL Auth environment, and must prove DB/REST/Realtime/Storage/Kong container IDs are unchanged. On any post-write failure, rollback must restore the original env and recreate Auth without the temporary override, then verify the original URL/issuer runtime values. Failed attempts are not success evidence. Public Auth health/settings and the canonical callback redirect remain required after a successful repair.

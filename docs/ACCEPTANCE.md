# Acceptance

## Automated status
- Specialist directory: pgTAP and disposable API/Auth/Storage tests cover private source rows/documents, owner writes, a MIME-consistent minimal PDF, forged identity/status/audit denial, ADMIN-only review, DRAFT publication denial, published-profile blocking after its medical rule is disabled, qualification revocation with unpublishing, blocked-owner invariants, atomic failure/audit behavior, and bounded anonymous search plus single-card lookup. VERIFIED medical confirmation/publication requires a normalized active rule in the normalized country for every medical service; unrelated, inactive, wrong-country, or partial rules fail closed without making the decision, while blocking and withdrawal do not depend on rules remaining active. Chromium runs the real mobile owner → submit → ADMIN publishes as VERIFIED → public search/card → ADMIN withdrawal to PENDING flow. It verifies the UI selects a non-VERIFIED qualification, the successful response and stored statuses, disappearance from search and direct card, hidden contacts, and absence of horizontal overflow.
- Registration fixtures wait for the successful `/auth?check=email` redirect before opening the sign-in form. A click alone does not confirm completion of the sign-up server action; signup errors fail at this preparation step.
- Unit validation: implemented.
- Clean temporary Supabase migration/RLS/Realtime integration: implemented as mandatory CI job without cloud credentials or silent skips; previously passed clean migrations and the prior 20 pgTAP assertions at `15b2a8e`, but requires a new full run for the corrected head.
- pgTAP: 31 schema/RLS/grant/default-privilege/publication assertions configured; NOT_RUN locally because Docker is unavailable.
- Auth/API/Realtime: 10 behavioral tests configured with isolated real user sessions in Node, including nonce conflicts, concurrent per-author quota, anonymous EXECUTE denial, PostgreSQL `system` readiness, two-second forbidden-event observation, denied C1 retry after reassignment, and self-contained open-socket reassignment/staff-role revocation scenarios; NOT_RUN for the corrected head locally because Docker is unavailable.
- Chromium: 4 production-build browser scenarios configured. The two-user 360 px scenario independently covers registration/session isolation, request/chat/refresh, foreign-request denial, Quick Exit, ordinary Back, removal of private chat and draft text, explicit return, `/cabinet` and old-card denial, and rejection without insertion of a public-form submission after session removal. It records whether Back restored the private document, loaded a new document, or did not revisit the replaced private history entry; every path must keep private content absent, retain the Quick Exit marker's effect, and deny the old session and access. `/help` deliberately remains public. Private chat is **not** required or claimed to enter BFCache: Chromium may reject a `no-store` document because of WebSocket and `no-store` subresource activity, and CDP reasons are correlated by frame/loader with the target navigation rather than treated as one proven cause. A separate cacheable, test-only HTML fixture imports the exact production guard string (it is not a production route) and unconditionally requires a real same-document `pageshow.persisted=true` restoration before the guard returns it to `/safe`; no synthetic lifecycle event or fallback pass is accepted. This proves the guard on a BFCache restoration, not private-chat BFCache eligibility. The chat still tears down its channel and transport on `pagehide`; that is cleanup, not proof of eligibility. On a persisted return without Quick Exit it revalidates Auth and request access, establishes PostgreSQL-ready Realtime, and only then catches up history through RLS. With Quick Exit active it does not restart chat restoration. Safe diagnostics contain only phase, `persisted`, a route template, random document ID, Chromium BFCache arguments and target-navigation CDP reasons (flat list or tree); they are written to `test-results/bfcache-diagnostics.json`, summarized in the log, and uploaded on success or failure. Missing diagnostics is a CI error. The Playwright project uses the supported full `channel: "chromium"`, omits Playwright's default `--disable-back-forward-cache` argument and verifies the actual command line. The callback rejection remains independent.
- Chat catch-up regressions cover more than 100 rows, equal `created_at` values, a concurrent newer Realtime row, UUID deduplication and cancellation of stale responses.
- Local lint, typecheck, unit tests and production build must pass before publication; Docker-backed checks remain mandatory in CI and coverage is not weakened.

## Required behavioral run
On disposable Supabase create two users, two coordinators, one admin and explicitly fictional data. User A registers/signs in, creates request; admin queue exposes only number/category/city/urgency/status/time, assigns C1 with reason; A/C1 exchange idempotent messages; C1 adds step and valid status transition. User B direct/UI/API reads and mutations fail. Forged owner/author/role/assignment/status fail. Unassigned C2 fails. Reassign to C2: C1 history SELECT/message INSERT and subsequent Realtime delivery fail; C2 works. Reconnect queries after cursor and deduplicates nonce.

Catalog test publishes only verified organization+point; pending rows fail view and geo RPC. Known coordinates verify ordering/distance tolerance, radius and pagination. Denied location and tile error retain list. Confidential shelter insert with coordinates/address fails.

At 360px and keyboard-only verify labels, focus, contrast, loading/error/empty states. Quick exit must not wait for network, must neutralize Back/BFCache rendering, and warns browser history remains.

`location.replace('/safe')` intentionally replaces the protected history entry. Therefore Back after the real Quick Exit cannot, by itself, prove that the protected document was restored from BFCache; depending on the preceding Next.js client transitions and reload it can restore a document, load one anew, or target another history entry. The acceptance test records the observed path and verifies content, marker, session and access invariants rather than passing on the final URL alone. The earlier combined assertion additionally stored its evidence in `sessionStorage`, even though Quick Exit deliberately clears that storage. True BFCache guard acceptance consequently uses a separate preserved, cacheable fixture entry, while the product Quick Exit continues to use replacement navigation for safety.

## Before real cases
Not accepted until staging integration/e2e, staff CRUD UI, deletion operational SLA, verified local emergency/catalog data, coordinator staffing/hours/response expectation, incident response, security/privacy/legal review, backups and monitoring are approved.

## Staff workspace v1 access matrix
| Действие | Роль | UI/server validation | RPC/RLS | Ожидаемый результат |
|---|---|---|---|---|
| Открыть очередь | ADMIN | bounded page 1–1000, 20 строк | `assignment_queue`; ADMIN внутри RPC | Только номер, категория, город, срочность, статус, дата и текущее назначение; без содержания/чата |
| Назначить / переназначить | ADMIN | UUID, новый исполнитель, основание 3–500; pending блокирует submit | `staff_coordinators`, атомарный `assign_case` | Одно активное назначение; прежний координатор теряет RLS/Realtime-доступ |
| Открыть назначенные | COORDINATOR | bounded pagination | `coordinator_cases` по `auth.uid()` | Только активные назначения вызывающего координатора |
| Открыть карточку и чат | COORDINATOR | UUID маршрута | `can_access_case`, RLS сообщений/плана | Приватное содержание доступно только текущему координатору и владельцу |
| Изменить статус | COORDINATOR | UUID и DB enum; pending блокирует submit | атомарный `change_case_status` | Только разрешённый переход; ошибка ничего не изменяет |

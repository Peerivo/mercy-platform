import {describe,it,expect} from 'vitest';import {requestSchema,specialistProfileSchema,specialistReviewSchema,specialistSearchSchema} from '../lib/validation';
describe('help request validation',()=>{const valid={category:'FAMILY',country:'Тестландия',city:'Примерск',description:'Достаточно длинное тестовое описание.',urgency:'NORMAL',can_message:true,can_call:false,contact_window:'',external_contact:'',consent:true};it('accepts minimal non-medical request',()=>expect(requestSchema.safeParse(valid).success).toBe(true));it('requires explicit consent',()=>expect(requestSchema.safeParse({...valid,consent:false}).success).toBe(false));it('bounds private text',()=>expect(requestSchema.safeParse({...valid,description:'x'.repeat(5001)}).success).toBe(false))})
describe('specialist validation',()=>{it('normalizes bounded lists',()=>{const value=specialistProfileSchema.parse({display_name:'Анна',description:'',country:'Россия',city:'Москва',travel_area:'',specializations:'психолог, консультант',services:'консультация',languages:'русский',work_formats:'онлайн',contact_details:'',show_contacts:false});expect(value.specializations).toEqual(['психолог','консультант'])});it('bounds public pagination',()=>expect(specialistSearchSchema.safeParse({page:'1001'}).success).toBe(false));it('accepts the qualification-withdrawal transition payload',()=>expect(specialistReviewSchema.safeParse({id:'00000000-0000-4000-8000-000000000001',publication:'PENDING',qualification:'PENDING',reason:'Повторная проверка'}).success).toBe(true))})

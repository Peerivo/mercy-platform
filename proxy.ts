import { createServerClient } from "@supabase/ssr";import { NextResponse,type NextRequest } from "next/server";
export async function proxy(req:NextRequest){let res=NextResponse.next({request:req});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(url&&key){const s=createServerClient(url,key,{cookies:{getAll:()=>req.cookies.getAll(),setAll(cs){cs.forEach(c=>req.cookies.set(c.name,c.value));res=NextResponse.next({request:req});cs.forEach(c=>res.cookies.set(c.name,c.value,c.options))}}});await s.auth.getUser()}if(req.nextUrl.pathname.startsWith("/cabinet")||req.nextUrl.pathname.startsWith("/staff")){res.headers.set("Cache-Control","private, no-store");res.headers.set("X-Robots-Tag","noindex, nofollow")}return res}
export const config={matcher:["/cabinet/:path*","/staff/:path*","/auth/:path*"]};

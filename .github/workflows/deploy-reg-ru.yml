name: Deploy REG.RU production

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type DEPLOY to deploy main to REG.RU"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: mercy-reg-ru-production
  cancel-in-progress: false

jobs:
  deploy:
    if: ${{ inputs.confirm == 'DEPLOY' && github.ref == 'refs/heads/main' }}
    environment: production
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      SITE_URL: https://xn----htbcggcjkhwxk7j6bn.xn--p1ai
      IMAGE_NAME: mercy-platform
      CONTAINER_NAME: mercy
      REMOTE_DIR: /opt/peerivo/mercy
      REG_RU_HOST: ${{ secrets.REG_RU_HOST }}
      REG_RU_USER: ${{ secrets.REG_RU_USER }}
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
      RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      RESEND_FROM_EMAIL: ${{ secrets.RESEND_FROM_EMAIL }}
      FEEDBACK_TO_EMAIL: ${{ secrets.FEEDBACK_TO_EMAIL }}

    steps:
      - name: Checkout reviewed main
        uses: actions/checkout@v4

      - name: Validate required production secrets
        shell: bash
        env:
          REG_RU_SSH_KEY: ${{ secrets.REG_RU_SSH_KEY }}
        run: |
          set -euo pipefail
          missing=0
          for name in REG_RU_HOST REG_RU_USER REG_RU_SSH_KEY NEXT_PUBLIC_SUPABASE_URL RESEND_API_KEY RESEND_FROM_EMAIL FEEDBACK_TO_EMAIL; do
            if [ -z "${!name:-}" ]; then
              echo "::error::Missing production secret: ${name}"
              missing=1
            fi
          done
          if [ -z "${NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY:-}" ] && [ -z "${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}" ]; then
            echo "::error::Set either NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY or NEXT_PUBLIC_SUPABASE_ANON_KEY"
            missing=1
          fi
          test "${missing}" -eq 0

      - name: Build production image on GitHub runner
        shell: bash
        env:
          NEXT_PUBLIC_MAP_TILE_URL: https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
          NEXT_PUBLIC_MAP_ATTRIBUTION: © OpenStreetMap contributors
        run: |
          set -euo pipefail
          docker build             --build-arg NEXT_PUBLIC_SUPABASE_URL             --build-arg NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY             --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY             --build-arg NEXT_PUBLIC_SITE_URL="${SITE_URL}"             --build-arg NEXT_PUBLIC_MAP_TILE_URL             --build-arg NEXT_PUBLIC_MAP_ATTRIBUTION             --build-arg GIT_SHA="${GITHUB_SHA}"             --label org.opencontainers.image.revision="${GITHUB_SHA}"             --label org.opencontainers.image.source="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}"             -t "${IMAGE_NAME}:${GITHUB_SHA}"             .

          docker save "${IMAGE_NAME}:${GITHUB_SHA}" | gzip -1 > /tmp/mercy-image.tar.gz
          test -s /tmp/mercy-image.tar.gz

      - name: Prepare runtime environment file
        shell: bash
        run: |
          set -euo pipefail
          umask 077
          {
            printf 'NODE_ENV=production\n'
            printf 'GIT_SHA=%s\n' "${GITHUB_SHA}"
            printf 'NEXT_PUBLIC_SITE_URL=%s\n' "${SITE_URL}"
            printf 'NEXT_PUBLIC_SUPABASE_URL=%s\n' "${NEXT_PUBLIC_SUPABASE_URL}"
            printf 'NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=%s\n' "${NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY:-}"
            printf 'NEXT_PUBLIC_SUPABASE_ANON_KEY=%s\n' "${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}"
            printf 'RESEND_API_KEY=%s\n' "${RESEND_API_KEY}"
            printf 'RESEND_FROM_EMAIL=%s\n' "${RESEND_FROM_EMAIL}"
            printf 'FEEDBACK_TO_EMAIL=%s\n' "${FEEDBACK_TO_EMAIL}"
          } > /tmp/mercy.env

      - name: Prepare deployment script
        shell: bash
        run: |
          cat > /tmp/deploy-mercy.sh <<'REMOTE_SCRIPT'
          #!/usr/bin/env bash
          set -euo pipefail

          IMAGE_NAME="${1:?image name required}"
          IMAGE_TAG="${2:?image tag required}"
          REMOTE_DIR="${3:?remote dir required}"
          CONTAINER_NAME="${4:?container name required}"

          IMAGE="${IMAGE_NAME}:${IMAGE_TAG}"
          IMAGE_ARCHIVE="${REMOTE_DIR}/mercy-image.tar.gz"
          NEXT_ENV="${REMOTE_DIR}/.env.next"
          PROD_ENV="${REMOTE_DIR}/.env.production"
          PREVIOUS_ENV="${REMOTE_DIR}/.env.previous"

          start_container() {
            local image="${1:?image required}"
            local env_file="${2:?env file required}"
            docker run -d               --name "${CONTAINER_NAME}"               --restart unless-stopped               --read-only               --tmpfs /tmp:rw,noexec,nosuid,size=64m               --env-file "${env_file}"               -p 127.0.0.1:3100:3000               "${image}" >/dev/null
          }

          old_image=""
          if docker inspect "${CONTAINER_NAME}" >/dev/null 2>&1; then
            old_image="$(docker inspect -f '{{.Image}}' "${CONTAINER_NAME}")"
          fi

          rollback() {
            trap - ERR
            set +e
            echo "Deployment failed; rolling back."
            docker rm -f "${CONTAINER_NAME}" >/dev/null 2>&1 || true
            if [ -n "${old_image}" ]; then
              rollback_env="${PROD_ENV}"
              if [ -f "${PREVIOUS_ENV}" ]; then
                rollback_env="${PREVIOUS_ENV}"
              fi
              start_container "${old_image}" "${rollback_env}" || true
            fi
            exit 1
          }
          trap rollback ERR

          cd "${REMOTE_DIR}"
          test -s "${IMAGE_ARCHIVE}"
          test -s "${NEXT_ENV}"

          if [ -f "${PROD_ENV}" ]; then
            cp -f "${PROD_ENV}" "${PREVIOUS_ENV}"
            chmod 600 "${PREVIOUS_ENV}"
          fi
          mv -f "${NEXT_ENV}" "${PROD_ENV}"
          chmod 600 "${PROD_ENV}"

          gzip -dc "${IMAGE_ARCHIVE}" | docker load >/dev/null

          if docker inspect "${CONTAINER_NAME}" >/dev/null 2>&1; then
            docker rm -f "${CONTAINER_NAME}" >/dev/null
          fi

          start_container "${IMAGE}" "${PROD_ENV}"

          healthy=false
          for _ in $(seq 1 45); do
            status="$(docker inspect -f '{{if .State.Health}}{{.State.Health.Status}}{{else}}none{{end}}' "${CONTAINER_NAME}")"
            if [ "${status}" = "healthy" ]; then
              healthy=true
              break
            fi
            if [ "${status}" = "unhealthy" ]; then
              break
            fi
            sleep 2
          done

          if [ "${healthy}" != "true" ]; then
            docker logs --tail=120 "${CONTAINER_NAME}" >&2 || true
            false
          fi

          curl --fail --silent --show-error http://127.0.0.1:3100/health >/dev/null

          trap - ERR
          rm -f "${IMAGE_ARCHIVE}"
          docker image prune -f >/dev/null
          echo "Mercy ${IMAGE_TAG} is healthy on 127.0.0.1:3100"
          REMOTE_SCRIPT
          chmod 700 /tmp/deploy-mercy.sh

      - name: Configure SSH
        shell: bash
        env:
          REG_RU_SSH_KEY: ${{ secrets.REG_RU_SSH_KEY }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "${REG_RU_SSH_KEY}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "${REG_RU_HOST}" >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Upload image and runtime configuration
        shell: bash
        run: |
          set -euo pipefail
          remote="${REG_RU_USER}@${REG_RU_HOST}"
          ssh "${remote}" "mkdir -p '${REMOTE_DIR}' && chmod 700 '${REMOTE_DIR}'"
          scp -q /tmp/mercy-image.tar.gz "${remote}:${REMOTE_DIR}/mercy-image.tar.gz"
          scp -q /tmp/mercy.env "${remote}:${REMOTE_DIR}/.env.next"
          scp -q /tmp/deploy-mercy.sh "${remote}:${REMOTE_DIR}/deploy-mercy.sh"

      - name: Deploy with health check and rollback
        shell: bash
        run: |
          set -euo pipefail
          remote="${REG_RU_USER}@${REG_RU_HOST}"
          ssh "${remote}"             "chmod 700 '${REMOTE_DIR}/deploy-mercy.sh' && '${REMOTE_DIR}/deploy-mercy.sh' '${IMAGE_NAME}' '${GITHUB_SHA}' '${REMOTE_DIR}' '${CONTAINER_NAME}'"

      - name: Verify remote container
        shell: bash
        run: |
          set -euo pipefail
          remote="${REG_RU_USER}@${REG_RU_HOST}"
          ssh "${remote}"             "docker ps --filter name=^/${CONTAINER_NAME}$ --format '{{.Names}} {{.Status}}' && curl --fail --silent --show-error http://127.0.0.1:3100/health"

      - name: Remove sensitive runner files
        if: always()
        shell: bash
        run: |
          rm -f ~/.ssh/id_ed25519 /tmp/mercy.env /tmp/deploy-mercy.sh /tmp/mercy-image.tar.gz

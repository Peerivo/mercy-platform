export const quickExitGuard = String.raw`(()=>{const marker="mercy_quick_exit";const isAuth=()=>location.pathname==="/auth";const marked=()=>{try{return sessionStorage.getItem(marker)==="1"}catch{return false}};const protect=()=>{if(!marked()||isAuth()){document.documentElement.removeAttribute("data-mercy-quick-exit-guard");return}document.documentElement.setAttribute("data-mercy-quick-exit-guard","1");location.replace("/auth")};const navigation=performance.getEntriesByType("navigation")[0];if(navigation&&navigation.type==="back_forward")protect();addEventListener("pageshow",event=>{if(event.persisted)protect()});addEventListener("popstate",protect)})();`;

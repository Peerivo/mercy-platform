"use server";
import {redirect} from "next/navigation";import {serverSupabase} from "@/lib/supabase/server";import {offerSchema} from "@/lib/validation";
export async function createOffer(fd:FormData){const parsed=offerSchema.safeParse({category:fd.get("category"),country:fd.get("country"),city:fd.get("city")??"",online:fd.get("online")==="on",description:fd.get("description"),contact_method:fd.get("contact_method"),consent:fd.get("consent")==="on"});if(!parsed.success)redirect("/volunteer?error=validation");const s=await serverSupabase(),{data:{user}}=await s.auth.getUser();if(!user)redirect("/auth");const payload={...parsed.data};delete (payload as Partial<typeof payload>).consent;const {error}=await s.rpc("create_volunteer_offer",{payload,consent_version:"volunteer-offer-v1"});if(error)redirect("/volunteer?error=save");redirect("/cabinet?offer=sent")}

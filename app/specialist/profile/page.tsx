import {redirect} from "next/navigation";import {serverSupabase} from "@/lib/supabase/server";import {saveSpecialist,submitSpecialist} from "./actions";
const csv=(v:string[]|undefined)=>v?.join(", ")||"";
export default async function Profile({searchParams}:{searchParams:Promise<Record<string,string>>}){const params=await searchParams,s=await serverSupabase(),{data:{user}}=await s.auth.getUser();if(!user)redirect("/auth");const {data:p}=await s.from("specialist_profiles").select("*").eq("account_id",user.id).maybeSingle();return <section className="container section"><h1>Профиль специалиста</h1><p>Анкета не подтверждает квалификацию. После отправки публикацию проверит модератор.</p>{params.error&&<p role="alert">Не удалось сохранить данные. Проверьте поля и повторите.</p>}{params.saved&&<p>Черновик сохранён.</p>}{params.submitted&&<p>Профиль отправлен на модерацию.</p>}<p className="notice">Публикация: {p?.publication_status||"DRAFT"}. Квалификация: {p?.qualification_status||"UNVERIFIED"}.</p><form action={saveSpecialist} className="card grid"><label>Имя для каталога<input name="display_name" required maxLength={120} defaultValue={p?.display_name}/></label><label>О себе<textarea name="description" maxLength={3000} defaultValue={p?.description}/></label><div className="grid cols2"><label>Страна<input name="country" required maxLength={80} defaultValue={p?.country}/></label><label>Город<input name="city" required maxLength={120} defaultValue={p?.city}/></label></div><label>Зона выезда<input name="travel_area" maxLength={300} defaultValue={p?.travel_area}/></label><label>Специализации через запятую<input name="specializations" defaultValue={csv(p?.specializations)}/></label><label>Услуги через запятую<input name="services" defaultValue={csv(p?.services)}/></label><label>Языки через запятую<input name="languages" defaultValue={csv(p?.languages)}/></label><label>Форматы работы через запятую<input name="work_formats" defaultValue={csv(p?.work_formats)}/></label><label>Контакты<input name="contact_details" maxLength={500} defaultValue={p?.contact_details}/></label><label><input type="checkbox" name="show_contacts" defaultChecked={p?.show_contacts}/> Показывать контакты в опубликованной карточке</label><button className="btn">Сохранить черновик</button></form>{p&&["DRAFT","REJECTED"].includes(p.publication_status)&&<form action={submitSpecialist}><button className="btn secondary">Отправить на модерацию</button></form>}</section>}

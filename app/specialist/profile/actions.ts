"use server";
import {redirect} from "next/navigation";import {serverSupabase} from "@/lib/supabase/server";import {specialistProfileSchema} from "@/lib/validation";
export async function saveSpecialist(fd:FormData){const s=await serverSupabase();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/auth");const parsed=specialistProfileSchema.safeParse({display_name:fd.get("display_name"),description:fd.get("description")||"",country:fd.get("country"),city:fd.get("city"),travel_area:fd.get("travel_area")||"",specializations:fd.get("specializations")||"",services:fd.get("services")||"",languages:fd.get("languages")||"",work_formats:fd.get("work_formats")||"",contact_details:fd.get("contact_details")||"",show_contacts:fd.get("show_contacts")==="on"});if(!parsed.success)redirect("/specialist/profile?error=validation");const {error}=await s.rpc("save_specialist_profile",{payload:parsed.data});if(error)redirect("/specialist/profile?error=save");redirect("/specialist/profile?saved=1")}
export async function submitSpecialist(){const s=await serverSupabase();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/auth");const {error}=await s.rpc("submit_specialist_profile");if(error)redirect("/specialist/profile?error=submit");redirect("/specialist/profile?submitted=1")}

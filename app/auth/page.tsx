import {reset,signIn,signUp} from "./actions";
export default async function Auth({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;return <section className="page-shell section"><h1>Вход и регистрация</h1>{q.error&&<p role="alert">Не удалось выполнить действие. Проверьте данные.</p>}{q.check&&<p>Проверьте email: подтверждение зависит от настроек проекта Supabase.</p>}<div className="grid cols2"><form className="card grid" action={signIn}><h2>Войти</h2><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Пароль<input name="password" type="password" minLength={10} required/></label><button className="btn">Войти</button></form><form className="card grid" action={signUp}><h2>Создать аккаунт</h2><p>Email нужен для входа, поэтому аккаунт не является полностью анонимным. Настоящее имя, телефон и адрес не нужны.</p><label>Email<input name="email" type="email" required/></label><label>Пароль от 10 символов<input name="password" type="password" minLength={10} required/></label><button className="btn">Зарегистрироваться</button></form></div><form action={reset} className="section"><label>Email для восстановления<input name="email" type="email" required/></label><button className="btn secondary">Восстановить доступ</button></form></section>}
